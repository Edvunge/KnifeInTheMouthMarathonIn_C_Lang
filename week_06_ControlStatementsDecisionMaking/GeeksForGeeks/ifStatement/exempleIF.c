#include <stdio.h>

int main(void) {
    int gfg = 9;

    if (gfg < 10) {
        printf("%d is less than 10", gfg);
    }

    if (gfg > 20) {
        printf("%d is greater than 20", gfg);
    }
    return (0);
}