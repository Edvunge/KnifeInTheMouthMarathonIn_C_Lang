
int foo() {
    /*
    a large
    amount of code 
    with multi-line comments
    ......
    */
    int someTest = 9;
    
    if (someTest) {
        // some more comments
        return 1;
    }
    return (0);
}

/* code from here on is "uncommented" (included in compiled executable) */