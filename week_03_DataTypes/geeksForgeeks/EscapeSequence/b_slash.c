#include <stdio.h>

int main(void) {

    // \b - backspace character transfers
    // the cursor one character back with
    // or with deleting on different
    // compilers.

    printf("Hello \b\b\b\b\b\bHi Geeks");

    return (0);
}