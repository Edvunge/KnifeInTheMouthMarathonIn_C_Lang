#include <stdio.h>

int main(void) {

    int a = 15;
    int b = 5;

    if (!(a > b))
        printf("b is greater than a\n");    
    else 
        printf("a is greater than b");

    return (0);
}